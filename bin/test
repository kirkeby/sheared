#!/usr/bin/env python

import unittest

import sys
sys.path.insert(0, '.')

from tests import reactor, coroutine

runner = unittest.TextTestRunner()
runner.run(reactor.suite)
runner.run(coroutine.suite)
